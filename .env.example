# Copy to .env and fill in values (do not commit .env)

# Token used to call the AI endpoint.
GITHUB_TOKEN=

# OpenAI-compatible base URL (include /v1 if your provider requires it)
AI_BASE_URL=https://models.inference.ai.azure.com/v1

# Model name
AI_MODEL=

# Optional override; if set, used instead of GITHUB_TOKEN
AI_API_KEY=
