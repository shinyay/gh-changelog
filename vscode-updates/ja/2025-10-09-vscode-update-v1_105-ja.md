---
title: "2025年9月リリース (version 1.105)"
date: "2025-10-09"
version_ref: "v1_105"
source_url: "https://code.visualstudio.com/updates/v1_105"
lang: "ja"
---

# 2025年9月リリース (version 1.105)

## リリース概要

VS Code 1.105 は GitHub Universe 2025 に合わせたリリースであり、**AI エージェント体験の進化**、**OS レベルの統合強化**、**開発者生産性の向上** を3本柱とする。Plan agent の導入によりタスクの事前計画が可能になり、Handoffs とサブエージェントで複雑なワークフローの構築を実現した。OpenAI Codex と Copilot CLI との Agent Sessions ビュー統合も新たに追加された。MCP マーケットプレイスのプレビュー、macOS ネイティブ認証ブローカーの対応、GPT-5-Codex と Claude Sonnet 4.5 モデルのサポートなど、多数の改善が含まれる。セキュリティ更新と Update 1.105.1 の修正リリースも提供されている。

## 注目の新機能・変更点

1. **Plan agent の導入** — 複雑なタスクをステップバイステップで事前計画し、実装に移行できる新しいビルトインエージェント。現時点は VS Code Insiders のみ。
2. **Handoffs によるチャットモード間のワークフロー構築** — チャットモード間でコンテキストとプロンプトを引き継ぐガイド付き遷移が可能に。
3. **Isolated subagents** — チャットセッション内で独立したコンテキストウィンドウを持つサブエージェントにタスクを委譲。
4. **OpenAI Codex / Copilot CLI との統合** — Agent Sessions ビューで OpenAI Codex と Copilot CLI のセッションを一元管理。
5. **MCP マーケットプレイス (Preview)** — GitHub MCP レジストリと連携し、MCP サーバーを Extensions ビューから直接閲覧・インストール可能に。
6. **AI によるマージコンフリクト解決** — Git マージコンフリクトマーカーを検出し、エージェントフローで自動解決を提案。
7. **macOS ネイティブ認証ブローカー対応** — macOS M-series デバイスで Microsoft 認証のネイティブブローカーをサポート。
8. **GPT-5-Codex / Claude Sonnet 4.5 モデル追加** — エージェントコーディングに最適化された最新モデルが利用可能に。
9. **OS 通知の拡張** — チャット応答とタスク完了時に OS レベルの通知バッジ・トーストを表示。
10. **完全修飾ツール名** — プロンプトファイルとチャットモードでツール名の名前空間衝突を回避する完全修飾名をサポート。

## セクション別詳細解説

### VS Code at GitHub Universe

#### 概要
GitHub Universe 2025 で発表された VS Code の主要アップデート。Plan agent、Handoffs、サブエージェント、OpenAI Codex 統合、Copilot CLI 統合の5つの新機能が紹介された。いずれも VS Code Insiders で先行提供。

#### 機能詳細

#### Plan agent

> **注意:** この機能は現時点で [VS Code Insiders](https://code.visualstudio.com/insiders/) でのみ利用可能。

複雑なコーディングタスクの事前計画を支援するビルトインエージェント。Chat ビューのエージェントドロップダウンから **Plan** を選択して使用。

- タスクを分析し、ステップに分解して実装計画を生成
- 計画の確定後、即時実装の開始または後で使用するための保存が可能
- 新しい Handoffs 機能と連携して動作

> 📷 [画像: Plan agent のプロンプト画面](https://code.visualstudio.com/assets/updates/1_105/plan-agent-sample.png)

詳細は [built-in plan agent in VS Code](https://code.visualstudio.com/docs/copilot/chat/chat-planning) を参照。

**ステータス**: **[Preview]**（Insiders のみ）
**対象ユーザー**: 複雑なタスクの事前計画を行いたいユーザー

#### Handoffs

> **注意:** この機能は現時点で [VS Code Insiders](https://code.visualstudio.com/insiders/) でのみ利用可能。

チャットモード間のガイド付き遷移を定義するワークフロー機能。チャット応答完了後に Handoff ボタンが表示され、次のモードに関連コンテキストとプリフィルされたプロンプトで移行できる。

- カスタムチャットモード定義の Frontmatter に `handoffs` を指定
- `label`、`agent`、`prompt`、`send` プロパティで遷移を制御
- マルチステップ開発ワークフローのオーケストレーションに最適

```markdown
---
description: Generate an implementation plan
tools: ['search', 'fetch']
handoffs:
  - label: Start Implementation
    agent: implementation
    prompt: Now implement the plan outlined above.
    send: true
---
```

詳細は [using handoffs in custom chat modes](https://code.visualstudio.com/docs/copilot/customization/custom-chat-modes#_handoffs) を参照。

**ステータス**: **[Preview]**（Insiders のみ）
**対象ユーザー**: カスタムチャットモードでワークフローを構築する開発者

#### Isolated subagents

> **注意:** この機能は現時点で [VS Code Insiders](https://code.visualstudio.com/insiders/) でのみ利用可能。

チャットセッション内で独立したタスクを委譲できる自律エージェント。独自のコンテキストウィンドウを持ち、ユーザーの操作なしに動作する。

- リサーチ、分析、コンテキスト収集などのタスクに最適
- タスク完了後、結果をメインのチャットセッションに返却
- メインセッションのコンテキスト管理を最適化
- チャットプロンプトまたはカスタムチャットモードで `#runSubagent` ツールを参照して使用

詳細は [using subagents in VS Code](https://code.visualstudio.com/docs/copilot/chat/chat-sessions#_subagents) を参照。

**ステータス**: **[Preview]**（Insiders のみ）
**対象ユーザー**: コンテキスト管理を効率化したいユーザー

#### Integration with OpenAI Codex

> **注意:** この機能は現時点で [VS Code Insiders](https://code.visualstudio.com/insiders/) でのみ利用可能。

Agent Sessions ビューが OpenAI Codex と統合された。ローカルとクラウドベースのエージェントセッションを統合インターフェースで管理可能。

- Codex 拡張のインストール・設定後、Agent Sessions ビューに自動的に表示
- Copilot Pro+ サブスクリプションで認証・追加設定なしでアクセス可能

> 📷 [画像: OpenAI Codex セッションを表示する Agent Sessions ビュー](https://code.visualstudio.com/assets/updates/1_105/agent-sessions-codex.png)

**ステータス**: **[Preview]**（Insiders のみ）
**対象ユーザー**: OpenAI Codex を活用するユーザー

#### Integration with Copilot CLI

Agent Sessions ビューが GitHub Copilot CLI をサポート。ローカルとクラウドベースのセッションを統合インターフェースで管理可能。

- チャットエディタまたは統合ターミナルで CLI セッションの開始・再開
- `/delegate` コマンドでクラウドのコーディングエージェントに委譲
- エディタからのコンテキスト添付をサポート

> 📷 [画像: Copilot CLI セッションを表示する Agent Sessions ビュー](https://code.visualstudio.com/assets/updates/1_105/agent-sessions-copilot-cli.png)

**ステータス**: **[Preview]**（Insiders のみ）
**対象ユーザー**: Copilot CLI を利用するユーザー

### Chat

#### 概要
完全修飾ツール名の導入、カスタムモデル向けの編集ツール改善、ネスト AGENTS.md ファイルのサポート、UX 改善（OS 通知、Chain of thought、最近のセッション表示）、Agent Sessions ビューの強化、チャットターミナルプロファイル、Apple アカウントでのサインイン、GPT-5-Codex / Claude Sonnet 4.5 モデルの追加など、幅広い改善が行われた。

#### 機能詳細

#### Fully qualified tool names

プロンプトファイルとカスタムチャットモードで使用するツール名に完全修飾名をサポート。MCP サーバーや拡張機能が提供するツール間の名前衝突を回避する。

- 例: `codebase` → `search/codebase`、`list_issues` → `github/github-mcp-server/list_issues`
- 従来の表記も引き続き動作し、新しい名前への移行を促す Code Action が提供される

> 📷 [画像: 非修飾ツール名を更新する Code Action](https://code.visualstudio.com/assets/updates/1_105/qualified_tool_names.png)

**ステータス**: **[GA]**
**対象ユーザー**: プロンプトファイルやカスタムチャットモードを使用するユーザー

#### Improved edit tools for custom models

**設定**: `github.copilot.chat.customOAIModels`

[Bring Your Own Key (BYOK)](https://code.visualstudio.com/docs/copilot/customization/language-models#_bring-your-own-language-model-key) カスタムモデル向けの編集ツールセットが改善された。

- VS Code ビルトインツールとのより良い統合
- デフォルトツールの強化とカスタムモデル向けの最適ツールセット選択の「学習」メカニズムを追加
- OpenAI 互換モデル使用時は `github.copilot.chat.customOAIModels` で編集ツールリストを明示的に設定可能

**ステータス**: **[GA]**
**対象ユーザー**: BYOK カスタムモデルを使用するユーザー

#### Support for nested AGENTS.md files (Experimental)

**設定**: `chat.useNestedAgentsMdFiles`

ワークスペースルートの `AGENTS.md` サポートが GA に昇格（デフォルト有効）。さらに、サブフォルダ内のネスト `AGENTS.md` ファイルの実験的サポートが追加された。

- フロントエンドとバックエンドで異なる指示を提供するなど、コードベースの部分ごとに特化したコンテキストを設定可能
- `chat.useNestedAgentsMdFiles` 設定で有効化

**ステータス**: **[Experimental]**（ネスト AGENTS.md）/ **[GA]**（ルート AGENTS.md）
**対象ユーザー**: チーム共有のカスタマイズを行う開発者

#### OS notifications for chat responses

**設定**: `chat.notifyWindowOnResponseReceived`

VS Code ウィンドウがフォーカスされていない時にチャット応答を受信すると、OS バッジと通知トーストが表示されるようになった。

- 通知には応答のプレビューが含まれ、選択するとチャット入力にフォーカス
- `chat.notifyWindowOnResponseReceived` で動作を制御

> 📷 [画像: VS Code ウィンドウ非フォーカス時の OS 通知](https://code.visualstudio.com/assets/updates/1_105/chat-notification.png)

**ステータス**: **[GA]**
**対象ユーザー**: バックグラウンドでチャットを利用するユーザー

#### Chain of thought (Experimental)

**設定**: `chat.agent.thinkingStyle`

モデルの推論プロセスを展開可能なセクションとして表示する機能。GPT-5-Codex の導入に伴い、thinking tokens がチャット応答内に表示されるようになった。

> 📷 [画像: 展開可能なセクションとしての thinking tokens](https://code.visualstudio.com/assets/updates/1_105/chat-thinking-tokens.png)

**ステータス**: **[Experimental]**
**対象ユーザー**: モデルの推論プロセスを確認したいユーザー

#### Show recent chat sessions (Experimental)

**設定**: `chat.emptyState.history.enabled`

新しいチャットセッション作成時に、最近のローカルチャット会話を表示する機能。前回の続きからの再開や過去の会話の見直しが容易に。

- デフォルトでは無効。`chat.emptyState.history.enabled` で有効化

> 📷 [画像: アクティブセッションがない時の最近の会話表示](https://code.visualstudio.com/assets/updates/1_105/chat-history-on-empty.png)

**ステータス**: **[Experimental]**
**対象ユーザー**: チャット履歴を活用したいユーザー

#### Keep or undo changes during an agent loop

エージェントの処理中にファイル編集の保持・取り消しが可能になった。以前はエージェントの処理完了まで変更の操作ができなかったが、長時間タスク中でもより細かい制御が可能に。

**ステータス**: **[GA]**
**対象ユーザー**: エージェントモードを利用するすべてのユーザー

#### Keyboard shortcuts for navigating user chat messages

チャットセッション内で過去のプロンプトを上下にナビゲーションするキーボードショートカットが追加された。

- 前のプロンプトへ: `workbench.action.chat.previousUserPrompt`
- 次のプロンプトへ: `workbench.action.chat.nextUserPrompt`

**ステータス**: **[GA]**
**対象ユーザー**: チャットを頻繁に利用するユーザー

#### Chat Sessions view enhancements

**設定**: `chat.agentSessionsViewLocation`

Chat Sessions ビューの UI リファインメントとパフォーマンス改善。

- Status Bar トラッキング、コンテキストメニュー、リッチな説明文が引き続きサポート
- ビューヘッダの「+」ボタンで新規セッションを素早く作成

> 📷 [画像: + ボタンで新規セッションを作成する Chat Sessions ビュー](https://code.visualstudio.com/assets/updates/1_105/chat-sessions.png)

**ステータス**: **[GA]**
**対象ユーザー**: 複数のエージェントセッションを管理するユーザー

#### Delegating to remote coding agents

リモートコーディングエージェントへの委譲体験が改善された。

- [Copilot coding agent が有効](https://aka.ms/coding-agent-docs) なリポジトリでは、**Delegate to coding agent** ボタンがデフォルトで表示
- 委譲時にファイル参照を含むすべてのコンテキストがフォワード
- 会話がコンテキストウィンドウを超える場合、VS Code が自動的に要約・圧縮

> 📷 [画像: Delegate to coding agent ボタン](https://code.visualstudio.com/assets/updates/1_105/delegate-button.png)

**ステータス**: **[GA]**
**対象ユーザー**: リモートコーディングエージェントにタスクを委譲するユーザー

#### Chat terminal profiles

**設定**: `chat.tools.terminal.terminalProfile.windows`、`chat.tools.terminal.terminalProfile.osx`、`chat.tools.terminal.terminalProfile.linux`

run-in-terminal ツールが起動するシェルを独立して設定できるプラットフォーム固有の設定が追加された。

- チャット用シェルでインタラクティブ要素をシンプル化し、通常のシェル環境はそのまま維持
- パス、引数、環境変数を個別に設定可能

```jsonc
"chat.tools.terminal.terminalProfile.osx": {
  "path": "bash",
  "args": [],
  "env": {
    "COPILOT": "1"
  }
}
```

**ステータス**: **[GA]**
**対象ユーザー**: エージェントモードのターミナル設定をカスタマイズしたいユーザー

#### Auto-reply to terminal prompts (Experimental)

**設定**: `chat.tools.terminal.autoReplyToPrompts`

エージェントがターミナルの入力プロンプト（例: `Confirm? y/n`）に自動的に応答する設定。オプトイン方式。

**ステータス**: **[Experimental]**
**対象ユーザー**: エージェントのターミナル操作を自動化したいユーザー

#### Terminal free-form input request detection

ターミナルでフリーフォーム入力が必要な場合に確認プロンプトを表示する機能。入力が必要な時のみ注意を引くことで、作業の集中を維持する。

**ステータス**: **[GA]**
**対象ユーザー**: エージェントモードでターミナルを利用するユーザー

#### Sign in with Apple accounts

GitHub または Google アカウントに加え、Apple アカウントでのサインインまたは GitHub Copilot アカウントのセットアップが可能になった。

> 📷 [画像: Apple アカウントでのサインインオプション](https://code.visualstudio.com/assets/updates/1_105/apple-sign-in.png)

詳細は [GitHub blog post](https://github.blog/changelog/2025-10-07-github-now-supports-social-login-with-apple/) を参照。

**ステータス**: **[GA]**
**対象ユーザー**: Apple アカウントを利用するユーザー

#### Model availability

新しいモデルがチャットでサポートされた:

- **GPT-5-Codex** — OpenAI の GPT-5 モデル、エージェントコーディングに最適化
- **Claude Sonnet 4.5** — Anthropic の最先端モデル、コーディングとリアルワールドエージェント向け

チャットのモデルピッカーで切り替え可能。

**ステータス**: **[GA]**
**対象ユーザー**: 最新モデルを活用したいすべてのユーザー

### MCP

#### 概要
MCP マーケットプレイスのプレビュー導入、サーバーの自動起動、リソース表現の改善、MCP 仕様のアップデート（アイコン、エリシテーションのデフォルト値）が行われた。

#### 機能詳細

#### MCP marketplace (Preview)

**設定**: `chat.mcp.gallery.enabled`

[GitHub MCP レジストリ](https://github.com/mcp) と連携し、Extensions ビューから直接 MCP サーバーを閲覧・インストールできるビルトイン MCP マーケットプレイス。

- デフォルトでは無効。MCP サーバー未インストール時にウェルカムビューで有効化を案内
- `@mcp` フィルタまたは **MCP Servers** フィルタドロップダウンで閲覧
- 名前による検索に対応

> 📷 [画像: MCP Servers ウェルカムビュー](https://code.visualstudio.com/assets/updates/1_105/mcp-servers-welcome.png)
> 📷 [画像: GitHub MCP サーバーの詳細画面](https://code.visualstudio.com/assets/updates/1_105/mcp-server-editor.png)

**ステータス**: **[Preview]**
**対象ユーザー**: MCP サーバーを活用するすべてのユーザー

#### Autostart MCP servers

**設定**: `chat.mcp.autostart`

チャットメッセージ送信時に、新しいまたは古い MCP サーバーが自動的に起動されるようになった。

- ダイアログなどのインタラクションをトリガーせず、チャット内にインジケータを表示
- 自動起動がデフォルトで有効化されたことにより、MCP 提供拡張機能は最初のチャットメッセージ送信時まで遅延アクティベーション
- 拡張機能開発者向けに `mcpServerDefinitionProviders` contribution point に `when` 句をサポート

**ステータス**: **[GA]**
**対象ユーザー**: MCP サーバーを利用するすべてのユーザー

#### Improved representation of MCP resources returned from tools

ツール結果に含まれるリソースの表現が改善された。以前はモデルにリソース取得の明確な指示がなかったが、リソースコンテンツのプレビューと完全な内容の取得手順を含めるようになった。MCP ツール使用時のモデルパフォーマンスが向上。

**ステータス**: **[GA]**
**対象ユーザー**: MCP ツールを利用するユーザー

#### MCP specification updates

MCP 仕様の以下のアップデートが採用された:

- **[SEP-973](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/955)** — MCP サーバーがデータに `icons` を関連付け可能に。サーバー、リソース、ツールにカスタムアイコンを設定できる。HTTP MCP サーバーはサーバーと同じ authority からアイコンを提供する必要があり、stdio サーバーは `file:///` URI を参照可能。

- **[SEP-1034](https://github.com/modelcontextprotocol/modelcontextprotocol/pull/1035)** — エリシテーション使用時に MCP サーバーが `default` 値を提供可能に。

**ステータス**: **[GA]**
**対象ユーザー**: MCP サーバー開発者

### Accessibility

#### 概要
Windows での PWsh シェル統合のスクリーンリーダー対応、チャットのアクセシビリティ改善、Accessible view の持続性が強化された。

#### 機能詳細

#### Shell integration for pwsh on Windows support for screen readers

PSReadLine がスクリーンリーダー検出時に無効化される問題に対応。シェル統合が PSReadLine に依存するため、スクリーンリーダーモードでストリームライン版の PSReadLine をアクティベートし、シェル統合機能を有効化。

**ステータス**: **[GA]**
**対象ユーザー**: Windows でスクリーンリーダーを使用する PowerShell ユーザー

#### Chat improvements

**設定**: `accessibility.verboseChatProgressUpdates`

チャットアクティビティに関するスクリーンリーダー向けの詳細なアナウンス機能を提供する設定が追加された。

- チャット入力から最後にフォーカスした応答アイテムにフォーカス可能

**ステータス**: **[GA]**
**対象ユーザー**: スクリーンリーダーを使用するユーザー

#### Accessible view persistence

VS Code と他のウィンドウ間を切り替えた際に、Accessible view 内のユーザー位置が維持されるようになった。

**ステータス**: **[GA]**
**対象ユーザー**: Accessible view を使用するユーザー

### Editor Experience

#### 概要
Quick Input のキーボードショートカットのオーバーライド対応と、NES（Next Edit Suggestions）のホワイトスペースのみの変更を無効化する設定が追加された。

#### 機能詳細

#### Override default shortcuts for Quick Input

コマンドパレット等で使用される Quick Input コントロール（Quick Pick、Input Box）のキーボードショートカットがコマンドに移行され、オーバーライド可能に。

- 例: `Tab` で Quick Pick の選択を確定するカスタマイズが可能
- Keyboard Shortcuts エディタで `quickInput.` を検索してすべてのショートカットを確認

**ステータス**: **[GA]**
**対象ユーザー**: キーボードショートカットをカスタマイズするユーザー

#### Disallow whitespace-only next edit suggestions

**設定**: `github.copilot.nextEditSuggestions.allowWhitespaceOnlyChanges`

NES（Next Edit Suggestions）でコードフォーマッティングのみの変更提案を無効化する設定。

**ステータス**: **[GA]**
**対象ユーザー**: NES を利用するユーザー

### Source Control

#### 概要
AI によるマージコンフリクト解決と、Source Control Graph からのファイルコミットのチャットコンテキスト追加が実装された。

#### 機能詳細

#### Resolve merge conflicts with AI

Git マージコンフリクトマーカーを含むファイルを開くと、AI によるコンフリクト解決が可能に。

- エディタの右下隅に新しいアクションが追加
- 選択すると Chat ビューが開き、マージベースと各ブランチの変更をコンテキストとしたエージェントフローが開始
- エディタ内で提案された解決案をレビュー可能
- `AGENTS.md` ファイルでマージコンフリクト解決をカスタマイズ可能

> 📷 [画像: エディタでのマージコンフリクト解決提案](https://code.visualstudio.com/assets/updates/1_105/merge-conflict-resolution.png)

**ステータス**: **[GA]**
**対象ユーザー**: Git を使用するすべての開発者

#### Add file commit to chat context

Source Control Graph ビューでコミットのファイルをチャットのコンテキストとして追加可能に。

- コミットを選択してファイル一覧を表示、右クリックで **Add to Chat** を選択
- 特定バージョンのファイル内容をチャットプロンプトのコンテキストとして提供

> 📷 [画像: コンテキストメニューの Add to Chat オプション](https://code.visualstudio.com/assets/updates/1_105/add-history-item-to-chat.png)

**ステータス**: **[GA]**
**対象ユーザー**: Git 履歴を活用したいユーザー

### Testing

#### 概要
テストのコードカバレッジレポート機能とテスト結果ビューのカラム入れ替えが追加された。

#### 機能詳細

#### Run tests with code coverage

チャットの `runTests` ツールがテストコードカバレッジをエージェントに報告するようになった。エージェントがコード全体をカバーするテストの生成・検証が可能に。

**ステータス**: **[GA]**
**対象ユーザー**: テスト駆動開発を行う開発者

#### Swap test result column

Test Results ビューで結果ツリーの表示位置を左右に切り替えるスワップボタンが追加された。

> 📷 [画像: スワップボタンが強調された Test Results ビュー](https://code.visualstudio.com/assets/updates/1_105/swap-test-result-column.png)

**ステータス**: **[GA]**
**対象ユーザー**: テスト結果ビューを利用するユーザー

### Tasks

#### 概要
長時間タスクの OS 通知とタスクターミナルタイトルの永続化が追加された。

#### 機能詳細

#### OS notification for long-running task completion

**設定**: `task.notifyWindowOnTaskCompletion`

VS Code ウィンドウが非フォーカス時に長時間タスクが完了すると、OS バッジと通知トーストが表示される。通知の選択でタスクが完了したウィンドウにフォーカス。

> 📷 [画像: タスク完了の OS 通知](https://code.visualstudio.com/assets/updates/1_105/task-notification.png)

**ステータス**: **[GA]**
**対象ユーザー**: ビルドタスクなどの長時間タスクを実行するユーザー

#### Task terminal title persistence

**設定**: `terminal.integrated.tabs.title`

タスク開始時にタスク名がターミナルタイトルとして永続化されるようになった。タスクが異なるプロセスを起動してもタイトルが変更されなくなった。

**ステータス**: **[GA]**
**対象ユーザー**: タスクを利用するユーザー

### Terminal

#### 概要
ターミナルのオーバーフローメニューに音声入力の開始アクションが追加された。

#### 機能詳細

#### Start dictation exposed

ターミナルのオーバーフローメニューに **Start dictation** アクションが追加された。音声ディクテーションによるターミナル入力が可能。対応する **Stop dictation** アクションも表示される。

> 📷 [画像: Start Dictation オプション付きのターミナルオーバーフローメニュー](https://code.visualstudio.com/assets/updates/1_105/start-dictation-terminal.png)

**ステータス**: **[GA]**
**対象ユーザー**: 音声入力を利用するユーザー

### Authentication

#### 概要
macOS でのネイティブ認証ブローカーサポートと GitHub 認証の PKCE サポートが追加された。

#### 機能詳細

#### macOS native broker support for Microsoft Authentication

**設定**: `microsoft-authentication.implementation`

最新の MSAL ライブラリの採用により、macOS でネイティブ認証体験が利用可能になった。

- M-series (ARM) macOS デバイスで利用可能
- Intune 登録済みでブローカーポリシーが有効な macOS マシン
- シングルサインオンフローを提供（推奨の認証方法）
- 問題がある場合は `microsoft-authentication.implementation` を `msal-no-broker` に変更してブラウザ認証にフォールバック

> 📷 [画像: macOS ネイティブブローカーダイアログ](https://code.visualstudio.com/assets/updates/1_105/macOS-auth-broker.png)

**ステータス**: **[GA]**
**対象ユーザー**: macOS M-series デバイスを使用するユーザー

#### PKCE support for GitHub Authentication

GitHub が [最近有効化した](https://github.blog/changelog/2025-07-14-pkce-support-for-oauth-and-github-app-authentication/) PKCE（[Proof Key for Code Exchange](https://www.rfc-editor.org/rfc/rfc7636)）を VS Code の GitHub 認証フローに採用。セキュリティが強化された。

**ステータス**: **[GA]**
**対象ユーザー**: GitHub 認証を使用するすべてのユーザー

### Languages

#### 概要
Python のテスト ID コピーアクションが追加された。

#### 機能詳細

#### Copy test ID action

実行ガターアイコンのコンテキストメニューに **Copy Test Id** コマンドが追加された。完全修飾された pytest または unittest のテスト識別子をコピー可能。

> 🎬 [動画: ガターコンテキストメニューの Copy Test Id コマンド](https://code.visualstudio.com/assets/updates/1_105/py_copy_test_id.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: Python テストを行う開発者

### Contributions to extensions

#### GitHub Pull Requests

[GitHub Pull Requests](https://marketplace.visualstudio.com/items?itemName=GitHub.vscode-pull-request-github) 拡張の新機能:

- `#openPullRequest` ツールがオープン PR の diff と PR ファイルを「オープン PR」として認識
- `githubIssues.issueAvatarDisplay` 設定で Issues ビューに最初の担当者のアバターまたは作成者のアバターを表示
- Pull Requests ビューのリフレッシュ時、リポジトリに新しい PR があるかチェックしてからクエリを実行（API 使用量の削減）

詳細は [changelog for the 0.120.0](https://github.com/microsoft/vscode-pull-request-github/blob/main/CHANGELOG.md#01200) を参照。

## 設定項目まとめ

### Chat

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `github.copilot.chat.customOAIModels` | カスタム OpenAI 互換モデルの設定 | — | GA |
| `chat.useNestedAgentsMdFiles` | ネスト AGENTS.md ファイルの有効化 | — | Experimental |
| `chat.notifyWindowOnResponseReceived` | チャット応答の OS 通知 | — | GA |
| `chat.agent.thinkingStyle` | Chain of thought の表示スタイル | — | Experimental |
| `chat.emptyState.history.enabled` | 新規セッション時の最近の会話表示 | `false` | Experimental |
| `chat.agentSessionsViewLocation` | Chat Sessions ビューの配置 | — | GA |
| `chat.tools.terminal.terminalProfile.windows` | Windows 用チャットターミナルプロファイル | — | GA |
| `chat.tools.terminal.terminalProfile.osx` | macOS 用チャットターミナルプロファイル | — | GA |
| `chat.tools.terminal.terminalProfile.linux` | Linux 用チャットターミナルプロファイル | — | GA |
| `chat.tools.terminal.autoReplyToPrompts` | ターミナルプロンプトへの自動応答 | — | Experimental |

### MCP

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `chat.mcp.gallery.enabled` | MCP マーケットプレイスの有効化 | `false` | Preview |
| `chat.mcp.autostart` | MCP サーバーの自動起動 | — | GA |

### Accessibility

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `accessibility.verboseChatProgressUpdates` | チャット進捗の詳細アナウンス | — | GA |

### Editor Experience

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `github.copilot.nextEditSuggestions.allowWhitespaceOnlyChanges` | NES のホワイトスペースのみの変更許可 | — | GA |

### Tasks

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `task.notifyWindowOnTaskCompletion` | 長時間タスク完了の OS 通知 | — | GA |
| `terminal.integrated.tabs.title` | ターミナルタブタイトルの設定 | `${process}` | GA |

### Authentication

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `microsoft-authentication.implementation` | Microsoft 認証の実装方式 | `msal` | GA |

## Breaking Changes / 非推奨化

このリリースでは明示的な Breaking Change は報告されていないが、以下の動作変更がある:

| 項目 | 内容 |
|---|---|
| MCP 自動起動のデフォルト有効化 | 拡張機能の即時アクティベーションが廃止され、最初のチャットメッセージ送信時まで遅延。MCP 拡張機能開発者は `when` 句の対応が推奨される |
| Delegate ボタンのデフォルト表示 | Copilot coding agent が有効なリポジトリで委譲ボタンがデフォルトで表示されるようになった |

## 開発者・拡張機能作成者向け

### Finalized APIs

#### Microsoft Authentication WWW-Authenticate claims challenges

Azure が create/delete 操作に MFA を強制するようになったことに対応。`vscode.authentication.getSession` に `wwwAuthenticate` と `fallbackScopes` を渡すことで、MFA チャレンジを処理可能に。

```ts
const wwwAuthenticateFromRequest = headers['WWW-Authenticate'];
vscode.authentication.getSession(
  'microsoft',
  {
    wwwAuthenticate: wwwAuthenticateFromRequest,
    fallbackScopes: scopesFromOriginalRequest
  },
  {
    createIfNone: true
  })
```

ARM API を使用する拡張機能は、`401 Unauthorized` + `WWW-Authenticate` ヘッダーのハンドリングが必要。Azure Resources 拡張は既に対応済み。

#### Prompt and instructions file contributions

拡張機能がプロンプトファイルとインストラクションファイルを contribute 可能に。

```json
{
  "contributes": {
    "chatPromptFiles": [
      {
        "name": "ReviewAndCreateIssue",
        "description": "Review the selected code and create an issue",
        "path": "./prompts/reviewAndCreateIssue.prompt.md"
      }
    ],
    "chatInstructions": [
      {
        "name": "TextMateGuidelines",
        "description": "Use these instructions when creating or modifying TextMate grammars",
        "path": "./prompts/textMateGuidelines.instructions.md"
      }
    ]
  }
}
```

Chat mode contributions（`chatModes`）は現在 Proposed API フラグの背後にある。

#### List keys in SecretStorage

Secret Storage に保存された全キーを一覧取得する API がファイナライズされた。

```ts
export function activate(context: ExtensionContext) {
  const keys: string[] = await context.secrets.keys();
  const value = await context.secrets.get(keys[0]);
}
```

`deactivate` 時にすべてのシークレットストレージデータを削除するなどのユースケースに対応。

## エンジニアリング・基盤改善

### Playwright VS Code MCP server

VS Code のローカルビルドを制御する MCP サーバーの探索が続けられている。

- スクリーンショット解析のモデル理解度には課題があったが、`#executePrompt` ツール（`github.copilot.chat.executePrompt.enabled` で有効化）を使用したサブエージェントオーケストレーションはコンテキスト汚染を効果的に防止
- [test/mcp フォルダ](https://github.com/microsoft/vscode/tree/main/test/mcp) から利用可能
- [Contribution Guidelines](https://github.com/microsoft/vscode/wiki/How-to-Contribute) に従ってローカル版 Code OSS を起動し、[プロンプトファイル](https://github.com/microsoft/vscode/blob/cd7de11f65cd5ff6a491f04fc013e363780bde31/.github/prompts/playwright.prompt.md) を使用

## Notable fixes

- [vscode#265842](https://github.com/microsoft/vscode/issues/265842) — Sonnet、Gemini、Grok モデルに影響するファイル破損の問題を修正
- [vscode#221255](https://github.com/microsoft/vscode/issues/221255) — 「Opening URIs can be insecure」警告の確認に関わらずターミナルリンクが開かれる問題を修正
- [vscode#229374](https://github.com/microsoft/vscode/issues/229374) — ターミナル OSC 8 ハイパーリンクのフォルダをネイティブファイルエクスプローラーではなく VS Code のエクスプローラーで開くように修正
- [vscode#268443](https://github.com/microsoft/vscode/issues/268443) — リリースノート内の設定リンクが機能しない問題を修正

## コミュニティ貢献

### 主な貢献者と貢献内容

| 貢献者 | 主な貢献 |
|---|---|
| **Simon Siefke** | メモリリーク修正3件（ReplAccessibilityAnnouncer、chat widget、folding）、メモリ使用量 ~1.2MB 削減 |
| **mawosoft (Matthias Wolf)** | PowerShell シェル統合の strict mode 対応修正と PSReadline キーリマッピング復元 |
| **avarayr** | macOS Tahoe でのウィンドウシャドウ無効化による GPU パフォーマンス問題の修正 |
| **CGNonofr (Loïc Mangeonjean)** | サポートされるタスクタイプの競合状態修正、Firefox でのクローンスタイルシート更新修正 |
| **hron (Aleksei Gusev)** | `diffEditor.revert` のキーボードバインディングを許可 |
| **DGideas (Wanlin Wang)** | カスタム OpenAI 互換モデル URL 解決の改善 (vscode-copilot-chat) |
| **johan-j (Johan Jansson)** | BYOK カスタムモデルのモデルピッカーグルーピング (vscode-copilot-chat) |

### Issue tracking 貢献者

- John Murray、Alberto Santini、RedCMD、Andrii Dieiev

### その他のリポジトリへの貢献

- **vscode-eslint**: realpath サポート、lintTask コマンド設定
- **vscode-json-languageservice**: propertyNames 向け examples 補完
- **vscode-python-environments**: conda 環境リフレッシュ修正、ターミナルエディタウィンドウでのアクティベートボタン表示

## まとめと所感

VS Code 1.105 は GitHub Universe 2025 を象徴するリリースであり、AI エージェント体験の基盤が大きく拡充された。Plan agent と Handoffs の組み合わせはタスクの計画から実装への移行をスムーズにし、サブエージェントはコンテキスト管理の課題に対する実践的な解決策を提供する。MCP マーケットプレイスのプレビューは MCP エコシステムの拡大を見据えた重要な一歩である。一方、Plan agent やサブエージェントなどの主要機能が VS Code Insiders 限定である点は留意が必要で、Stable チャネルのユーザーにとっては次回以降のリリースで恩恵を受けることになる。GPT-5-Codex と Claude Sonnet 4.5 の追加により、エージェントコーディングのモデル選択肢が実質的に広がった。
