---
title: "2025年10月リリース (version 1.106)"
date: "2025-11-12"
version_ref: "v1_106"
source_url: "https://code.visualstudio.com/updates/v1_106"
lang: "ja"
---

# 2025年10月リリース (version 1.106)

## リリース概要

VS Code 1.106 は **Agent HQ**、**セキュリティと信頼性**、**エディタ体験の改善** の3つの柱を掲げたリリースである。Agent Sessions ビューがデフォルトで有効化され、ローカル・リモート・CLI のエージェントセッションを一元管理できるようになった。Plan agent の導入により、複雑なタスクを実装前にステップバイステップで計画できる。セキュリティ面ではツール承認の粒度向上やターミナルツールのパーサー改善が行われ、エディタ体験ではアイコンの刷新、Terminal IntelliSense の GA 化、インライン提案のオープンソース化など多数の改善が含まれる。Update 1.106.1 / 1.106.2 / 1.106.3 の修正リリースも提供されている。

## 注目の新機能・変更点

1. **Agent Sessions ビューのデフォルト有効化** — ローカル・クラウド・CLI のエージェントセッションを一元管理するビューがデフォルトで有効になった。
2. **Plan agent の導入** — 複雑なタスクを実装前にステップバイステップで計画し、承認後に Copilot が実装する新しいワークフロー。
3. **インライン提案のオープンソース化** — GitHub Copilot のインライン提案が vscode-copilot-chat リポジトリに統合され、オープンソース化された。
4. **Terminal IntelliSense の GA 化** — 約1.5年間のプレビュー期間を経て、ターミナル IntelliSense が正式リリースとして段階的にロールアウト。
5. **チャットモードからカスタムエージェントへのリネーム** — Chat modes が Custom Agents に名称変更され、`.github/agents` ディレクトリで管理する形式に。
6. **ターミナルツールのパーサー改善** — tree-sitter ベースのパーサーが統合され、自動承認のコマンド検出精度が大幅に向上。
7. **MCP レジストリの組織ポリシーサポート** — GitHub Organization ポリシーを通じた MCP サーバーアクセス制御が可能に。
8. **アイコンの刷新** — codicon アイコンセットがモダンなデザインにリフレッシュ。
9. **Advanced Settings の概念導入** — 高度な設定を通常時は非表示にし、必要時にフィルタで表示する仕組み。
10. **Inline chat v2 (Preview)** — 軽量化されたインラインチャットのプレビュー版が導入。

## セクション別詳細解説

### Agents

#### 概要
Agent Sessions ビューのデフォルト有効化、Plan agent の導入、クラウド・CLI エージェントとの統合改善、カスタムエージェントへのリネームなど、エージェント体験全体を大幅に強化するセクション。

#### 機能詳細

#### Agent Sessions view

**設定**: `chat.agentSessionsViewLocation`

Agent Sessions ビューがデフォルトで有効化された。VS Code 内のローカルセッションと、Copilot coding agent、GitHub Copilot CLI、OpenAI Codex などのバックグラウンドエージェントセッションを一元管理できる。

- デフォルトではセッションをソース別にセクション分け（ローカル / バックグラウンドエージェント）
- `chat.agentSessionsViewLocation` を `single-view` に設定すると、全プロバイダを統合したビューに
- `single-view` オプションでは Agent Sessions ビューが Secondary Side Bar の Chat ビュー横に移動
- セッション一覧での検索（`Ctrl+F`）にも対応

> 📷 [画像: Primary Side Bar の Agent Sessions ビュー](https://code.visualstudio.com/assets/updates/1_106/agent-sessions-view.png)

> 🎬 [動画: 新しい Agent Sessions ビューの操作](https://code.visualstudio.com/assets/updates/1_106/agent-sessions.mp4)

> 🎬 [動画: Agent Sessions ビューでのセッション検索](https://code.visualstudio.com/assets/updates/1_106/sessionsPaneSearch.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: 複数のエージェントセッションを並行管理するすべてのユーザー

#### Plan agent

複雑なタスクの実装前に、ステップバイステップの計画を作成する新しいエージェント。Chat ビューのエージェントドロップダウンから **Plan** を選択して利用する。

- 明確化のための質問をユーザーに提示し、要件とコンテキストを事前に確認
- 詳細な実装計画を生成し、ユーザーが承認してから実装に進む
- 計画の反復的な改善（要件の調整、スコープの変更、未解決の質問への対応）を推奨
- 承認後は VS Code 内のローカル実装またはクラウドエージェント経由で実装
- **Configure Custom Agent** メニューからビルトインの Plan agent をコピーし、チーム固有のカスタム Plan agent を作成可能

> 🎬 [動画: Plan agent の動作](https://code.visualstudio.com/assets/updates/1_106/plan-mode.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: 複雑なタスクの事前計画を行いたいすべてのユーザー

#### Cloud agents

クラウドエージェントセッションに複数のアップデートが行われた。

- Copilot coding agent の統合が GitHub Pull Request 拡張から Copilot Chat 拡張に移行し、よりネイティブな体験を提供
- VS Code と GitHub Mission Control 間のシームレスな遷移（Agent Sessions ビューからブラウザでの直接オープンなど）が可能に

> 🎬 [動画: GitHub Mission Control と VS Code の連携](https://code.visualstudio.com/assets/updates/1_106/deeplink.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: クラウドエージェントを利用するユーザー

#### CLI agents

Copilot CLI との初期統合が導入された。

- チャットエディタまたは統合ターミナルで CLI エージェントセッションを新規作成・再開可能
- CLI エージェントエディタ内でメッセージ送信、モデル切り替え、コンテキスト添付が可能

> 📷 [画像: CLI エージェントのドロップダウンメニュー](https://code.visualstudio.com/assets/updates/1_106/cli-dropdown.png)
> 📷 [画像: CLI エージェントエディタと統合ターミナル](https://code.visualstudio.com/assets/updates/1_106/cli-editor-and-terminal.png)

**ステータス**: **[GA]**
**対象ユーザー**: Copilot CLI を利用するユーザー

#### Agent delegation

エージェントへのタスク委譲の体験が改善された。

- チャットパネルのクラウドボタンから委譲先エージェントを選択可能
- CLI から `/delegate` コマンドで Copilot coding agent に委譲可能

> 📷 [画像: CLI エージェントエディタからの委譲](https://code.visualstudio.com/assets/updates/1_106/cli-delegate-editor.png)

**ステータス**: **[GA]**
**対象ユーザー**: タスクをクラウドエージェントに委譲するユーザー

#### CLI agent edit tracking

バックグラウンドエージェント（Copilot CLI など）による編集がチャット編集セッションで追跡されるようになった。Agent Sessions ビューからセッションを作成すると、インライン編集ピルとワーキングセットビューの両方で変更を確認できる。

**ステータス**: **[GA]**
**対象ユーザー**: CLI エージェントを利用するユーザー

#### Chat modes renamed to custom agents

Chat modes が **Custom Agents** に名称変更された。

- 定義ファイルは `.github/agents` ディレクトリに配置
- `.agents.md` サフィックスを使用可能
- GitHub Copilot Cloud Agents および CLI Agents としても利用可能
- **Chat: New Custom Agent...** で新規作成、**Chat: Configure Custom Agents...** で管理
- 既存の `.chatmode.md` ファイル（`.github/chatmodes`）は引き続き動作し、自動的にカスタムエージェントとして扱われる
- エディタで開くと、カスタムエージェントファイルへの移行を促すクイックフィックスが表示

> 📷 [画像: エージェントピッカーコントロール](https://code.visualstudio.com/assets/updates/1_106/agent-picker.png)

**ステータス**: **[GA]**
**対象ユーザー**: カスタムエージェントを利用するすべてのユーザー

#### Custom agent metadata

カスタムエージェント `.agent.md` ファイルに新しい `target` frontmatter プロパティが追加された。

- `target: vscode` — ローカルチャット用に最適化。`name`、`description`、`argument-hint`、`model`、`tools`、`handoffs` をサポート
- `target: github-copilot` — Copilot クラウドエージェントまたは GitHub CLI 用。`name`、`description`、`tools`、`mcp-servers`、`target` をサポート
- すべてのエージェントはすべての環境で実行可能（不明な属性・ツールは無視される）
- `name` — ファイル名と異なるラベルを設定
- `argument-hint` — チャット入力にガイダンスを表示
- `handoffs` — 他のエージェントへのガイド付き遷移を設定し、マルチステップワークフローを構築
- エージェントファイルエディタでバリデーション、コード補完、ホバー、コードアクションを提供

> 📷 [画像: カスタムエージェントファイル編集時のバリデーション](https://code.visualstudio.com/assets/updates/1_106/agent-file-editing.gif)

**ステータス**: **[GA]**
**対象ユーザー**: カスタムエージェントを作成・管理する開発者

### Code Editing

#### 概要
diff エディタでの削除コードの選択可能化、インライン提案のオープンソース化、スヌーズ機能、Go to Line コマンドの強化など、コード編集体験の改善が行われた。

#### 機能詳細

#### Deleted code in diff editor is now selectable

diff エディタのインラインビューで、削除された行のテキストを選択・コピーできるようになった。以前は削除されたコードのテキストを選択する手段がなかった。

> 🎬 [動画: diff エディタでの削除行テキスト選択](https://code.visualstudio.com/assets/updates/1_106/diff-editor-select-deleted-lines-text.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: diff エディタを利用するすべてのユーザー

#### Inline suggestions are open source

GitHub Copilot Chat のオープンソース化に続き、インライン提案が vscode-copilot-chat リポジトリに統合されオープンソース化された。

- Chat 拡張機能がすべてのインライン提案を提供するように統合
- 変更はユーザーに対して透過的で、従来と同じコード提案が継続
- 問題が発生した場合は `chat.extensionUnification.enabled` 設定で一時的に元に戻すことが可能
- GitHub Copilot 拡張機能は2026年初頭に廃止予定

**ステータス**: **[GA]**
**対象ユーザー**: GitHub Copilot のインライン提案を利用するすべてのユーザー

#### Snooze inline suggestions from gutter

ガターアイコンからインライン提案を直接スヌーズできるようになった。ガターアイコンにホバーすると **Snooze** オプション付きのコントロールが表示され、一時停止する期間を選択できる。

> 📷 [画像: インライン提案のスヌーズボタン](https://code.visualstudio.com/assets/updates/1_106/nes-snooze.png)

**ステータス**: **[GA]**
**対象ユーザー**: インライン提案を利用するユーザー

#### Go to line improvements

**Go to Line** コマンド（`Ctrl+G`）に複数の改善が加えられた。

- `::` 構文で特定の文字位置へのナビゲーションをサポート（例: `::599` でファイル内の599文字目に移動）
- `::-100` でファイル末尾から100文字前に移動
- 1ベース（デフォルト）と0ベースのオフセット計算を入力ボックスのトグルで切り替え可能
- 範囲外の行番号はファイル最終行に移動
- 負のカラム番号で行末からのナビゲーション（例: `:12:-1` で12行目の最後の文字）
- カラム番号が行の長さを超える場合は行末に移動

> 🎬 [動画: Go to Line コマンドでのエディタナビゲーション](https://code.visualstudio.com/assets/updates/1_106/go-to-line.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: ファイル内ナビゲーションを頻繁に行うユーザー

### Editor Experience

#### 概要
codicon アイコンセットの刷新、Linux ポリシーサポート、マルチファイル diff エディタでの変更ナビゲーション、診断ホバーのコピーボタン、アクセント非依存のコマンドフィルタリング、高度な設定の概念導入が行われた。

#### 機能詳細

#### Refreshed iconography

codicon アイコンセットがリフレッシュされた。新しいアイコンはカーブ、新しいモディファイアデザイン、より正確なメタファーで洗練され、モダンで読みやすいデザインになった。

> 📷 [画像: 更新されたプロダクトアイコン](https://code.visualstudio.com/assets/updates/1_106/refreshed-iconography.png)

**ステータス**: **[GA]**
**対象ユーザー**: すべてのユーザー

#### Linux policy support

Linux システムでの JSON ファイルによる VS Code ポリシー管理が導入された。管理者が Linux マシン上のすべてのユーザーに対して特定の設定と構成を適用可能になる。

- 詳細は [JSON Policies on Linux](https://code.visualstudio.com/docs/setup/enterprise#_json-policies-on-linux) を参照

**ステータス**: **[GA]**
**対象ユーザー**: Linux 環境の管理者

#### Navigate changes in multi file diff editor

マルチファイル diff エディタで、ファイルをまたいで次・前の変更にナビゲートできるようになった。キーバインドまたはナビゲーション矢印キーで変更間を移動可能。

> 🎬 [動画: マルチファイル diff エディタでの変更ナビゲーション](https://code.visualstudio.com/assets/updates/1_106/multiFileDiffNav.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: コードレビューを行うユーザー

#### Copy diagnostic hover text

診断ホバー（エラー、警告、情報、ヒント）にコピーボタンが追加された。診断マーカーにホバーしてマウスをメッセージ上に移動すると、右上隅にコピーボタンが表示される。

> 📷 [画像: 診断ホバーのコピーボタン](https://code.visualstudio.com/assets/updates/1_106/hover-copy-button.png)

**ステータス**: **[GA]**
**対象ユーザー**: すべてのユーザー

#### Accent-insensitive command filtering

コマンドパレットでアクセント記号を無視した検索が可能になった。例えば `Générer` を含むコマンドを `generer` と入力して検索できる。

- キーボードレイアウトや言語によらず、コマンドを見つけやすくなる
- [Unicode Normalization Form D](https://www.unicode.org/reports/tr15/#Norm_Forms) に基づくフィルタリングで、すべての Unicode 言語をサポート

> 🎬 [動画: コマンドパレットでのアクセント非依存フィルタリング](https://code.visualstudio.com/assets/updates/1_106/filtering.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: 非英語キーボードレイアウトを使用するユーザー

#### Advanced VS Code settings

VS Code に **Advanced Settings** の概念が導入された。専門的なシナリオ向けの設定で、デフォルトでは Settings エディタに表示されない。

- Settings エディタのフィルタドロップダウンから **Advanced** を選択、または `@tag:advanced` で表示
- 設定名の完全一致検索や `@id:` フィルター使用時は Advanced フィルターなしでも結果に表示
- `@modified` や `@feature:` など他のフィルターとの組み合わせが可能（例: `@tag:advanced @feature:terminal`）
- 拡張機能作者は設定に `advanced` タグを追加して高度な設定としてマーク可能

> 📷 [画像: Advanced フィルターが適用された Settings エディタ](https://code.visualstudio.com/assets/updates/1_106/advanced-settings.png)

**ステータス**: **[GA]**
**対象ユーザー**: 高度な設定を利用するパワーユーザー、拡張機能作者

### Chat

#### 概要
ツール選択の改善、ツール承認と信頼の強化、ターミナルツールの大幅な改善（パーサー統合、ファイル書き込み検出、出力表示）、チャットセッションのプロンプト保存、Reasoning トークンの表示改善、Inline chat v2 プレビューなど、チャット体験全体が大幅に強化された。

#### 機能詳細

#### Embeddings-based tool selection

100以上のツールが有効なユーザー向けに、ツールのフィルタリングとグルーピングが大幅に改善された。「Optimizing tool selection...」のローディング状態が表示される頻度と時間が削減され、適切なツールが選択される精度も向上した。

**ステータス**: **[GA]**
**対象ユーザー**: 多数のツールを有効にしているユーザー

#### Tool approvals and trust

##### Post-approval for external data
外部データを取得するエージェントツールがポスト承認をサポートするようになった。潜在的なプロンプトインジェクション攻撃から保護するため、データがチャットセッションで使用される前にレビュー可能。

- `#fetch` ツールと `openWorldHint` を宣言する MCP ツールで有効

> 🎬 [動画: 外部データのポスト承認](https://code.visualstudio.com/assets/updates/1_106/post-approval.mp4)

##### Trust all tools for a server or extension
MCP サーバーと拡張機能ツールをソースレベルで一括信頼できるようになった。**Allow** ボタンのドロップダウンから、特定の MCP サーバーまたは拡張機能のすべてのツールを一度に承認可能。

- **Chat: Manage Tool Approval** コマンドでプレ承認とポスト承認の両方を管理可能に

> 📷 [画像: Manage Tool Approval コマンド](https://code.visualstudio.com/assets/updates/1_106/manage-tool-approvals.png)

##### Tool auto approval status moved
自動承認ステータスがチャットビュー内のインライン表示からツール呼び出しステータス/チックアイコンに移動。

> 📷 [画像: ツールのチックアイコンホバーで自動承認理由を表示](https://code.visualstudio.com/assets/updates/1_106/tool-auto-approve-hover.png)

**ステータス**: **[GA]**
**対象ユーザー**: エージェントモードでツールを利用するすべてのユーザー

#### Terminal tool

##### Auto approve parser improvements
ターミナルツールのサブコマンド検出に [tree-sitter](https://tree-sitter.github.io/tree-sitter/) パーサーが統合された。従来の単純な文字列分割方式から、[PowerShell grammar](https://github.com/airbus-cert/tree-sitter-powershell) や [bash grammar](https://github.com/tree-sitter/tree-sitter-bash) を使用した正確な解析に移行。

- `echo "a|b|c"` のような文字列内のパイプが誤検出されなくなった
- 括弧ペア、波括弧ペア、バッククォートの使用が適切に処理される
- zsh のセミコロンなど、bash と異なるシェル構文では検出漏れの可能性あり

> 📷 [画像: echo 呼び出し内の $() 検出](https://code.visualstudio.com/assets/updates/1_106/tool-terminal-parser.png)

##### File write/redirection detection (Experimental)

**設定**: `chat.tools.terminal.blockDetectedFileWrites`

新しいパーサーにより、リダイレクションによるファイル書き込みを検出可能に。ワークスペース外への書き込みがデフォルトで自動承認をブロックする。

> 📷 [画像: ワークスペース外書き込みによる自動承認ブロック](https://code.visualstudio.com/assets/updates/1_106/tool-terminal-redirection.png)

**ステータス**: **[Experimental]**

##### Disable default auto approve rules (Experimental)

**設定**: `chat.tools.terminal.ignoreDefaultAutoApproveRules`

デフォルトの自動承認ルール（許可・拒否の両方）を無効化する設定。デフォルトルールを調べる手間なく、より細かい制御が可能。

**ステータス**: **[Experimental]**

##### Shell specific prompts and command rewriting
ターミナルツールが PowerShell、bash、zsh、fish 向けのシェル固有の説明文を持つようになった。エージェントが提案するコマンドの信頼性が向上。

- PowerShell では `&&` を `;` に自動書き換え（Windows PowerShell v5 は `&&` チェーンオペレーターを未サポートのため）

##### Attach terminal commands to chat
ターミナルコマンドをコマンドデコレーションのコンテキストメニューからチャットにコンテキストとして添付可能に。コマンドライン、キャプチャされた出力、終了コードが共有され、コピー＆ペーストなしでトラブルシューティングをエスカレートできる。

> 🎬 [動画: ターミナルコマンドをチャットに添付](https://code.visualstudio.com/assets/updates/1_106/terminal-attachment.mp4)

##### View terminal output inside chat (Experimental)

**設定**: `chat.tools.terminal.outputLocation`

ターミナル出力の表示場所を制御する設定。デフォルトの `none` でターミナルパネルの散乱を防止。

- **Show Terminal** — 非表示セッションを表示・フォーカス
- **Show Output** — チャットビュー内にターミナル出力をインライン表示。非ゼロ終了コードのコマンドは自動展開

> 🎬 [動画: チャットビュー内でのターミナル出力表示](https://code.visualstudio.com/assets/updates/1_106/terminal-chat-output.mp4)

**ステータス**: **[Experimental]**

##### Discover hidden chat terminals (Experimental)
`chat.tools.terminal.outputLocation` が `none` の場合、ターミナルタブビューに **X hidden terminal(s)** ボタンが表示され、非表示チャットターミナルをクイックピックで一覧表示・フォーカス可能。

> 🎬 [動画: 非表示チャットターミナルの発見](https://code.visualstudio.com/assets/updates/1_106/terminal-chat-hidden.mp4)

**ステータス**: **[Experimental]**
**対象ユーザー**: エージェントモードでターミナルツールを利用するユーザー

#### Save conversation as prompt

`/savePrompt` コマンドでチャット会話を再利用可能なプロンプトとして保存できるようになった。会話に基づいた汎用プロンプトが生成され、ユーザーまたはワークスペースレベルの保存先を選択可能。

- 以前の `/save` コマンドを置き換え
- 生成されたプロンプトは将来のチャットセッションで再利用、またはチームと共有可能

> 📷 [画像: Save prompt ボタン](https://code.visualstudio.com/assets/updates/1_106/save-prompt-prompt.png)

**ステータス**: **[GA]**
**対象ユーザー**: チャットパターンを保存・共有したいユーザー

#### Edit welcome prompts

Chat ウェルカムビューのサジェストプロンプトを右クリック（または `Shift+F10`）して **Edit Prompt File** オプションにアクセスし、対応するプロンプトファイルを直接エディタで開けるようになった。

- `chat.promptFilesRecommendations` 設定で構成されたユーザー定義プロンプトおよびプロジェクト固有プロンプトで動作

> 📷 [画像: サジェストプロンプトのコンテキストメニュー](https://code.visualstudio.com/assets/updates/1_106/welcome-prompt-context.png)

**ステータス**: **[GA]**
**対象ユーザー**: カスタムプロンプトを利用するユーザー

#### Automatically open edited files

**設定**: `chat.openEditedFilesAutomatically`、`accessibility.openChatEditedFiles`

エージェントが編集したファイルを自動的にエディタで開くデフォルト動作が無効に変更された。以前の動作に戻すには `accessibility.openChatEditedFiles` を有効にする。

**ステータス**: **[GA]**（動作変更）
**対象ユーザー**: エージェントモードを利用するすべてのユーザー

#### Reasoning (Experimental)

**設定**: `chat.agent.thinkingStyle`、`chat.agent.thinking.collapsedTools`

チャットでの推論トークン（thinking tokens）表示が強化され、GPT-5-Codex、GPT-5、GPT-5 mini、Gemini 2.5 Pro でもサポートされるようになった。

- `chat.agent.thinkingStyle` が3つの一般的なスタイルに調整。デフォルトは `fixedScrolling`（最新の思考チェーンを表示）
- `chat.agent.thinking.collapsedTools` でツール呼び出しを折りたたみ可能な思考 UI に統合

> 📷 [画像: 推論トークンとインターリーブされたツール呼び出し](https://code.visualstudio.com/assets/updates/1_106/reasoning.png)

**ステータス**: **[Experimental]**
**対象ユーザー**: 最新モデルの推論プロセスを確認したいユーザー

#### Inline chat v2 (Preview)

**設定**: `inlineChat.enableV2`（`true` で有効化）

モダナイズされたインラインチャット。以下の設計原則で構築:

- 単一プロンプト
- 単一ファイル
- コード変更のみ

全体的に軽量な体験を提供し、対応できないタスクの場合は自動的に Chat ビューにアップグレードされる。

> 🎬 [動画: Inline chat v2 の動作](https://code.visualstudio.com/assets/updates/1_106/inline-chat.mp4)

**ステータス**: **[Preview]**
**対象ユーザー**: インラインチャットを利用するユーザー
**有効化方法**: `inlineChat.enableV2` を `true` に設定

#### Chat view UX improvements

Chat ビューの UX が改善された。

- 新しいチャット作成アクションがドロップダウンに変更（エディタ内またはウィンドウでのセッション作成オプション）
- ツールと MCP サーバーアクションがモデルピッカーの隣に移動
- 設定ドロップダウンの整理
- チャットビューの数式表現を右クリックしてソースをコピー可能に

> 📷 [画像: 新しいチャット作成ドロップダウン](https://code.visualstudio.com/assets/updates/1_106/chat-view.png)

**ステータス**: **[GA]**
**対象ユーザー**: すべてのチャットユーザー

### MCP

#### 概要
GitHub Organization ポリシーによる MCP サーバーアクセス制御、ワークスペース構成へのインストール、OAuth 認証フローの強化（CIMD、WWW-Authenticate スコープステップアップ）が行われた。

#### 機能詳細

#### MCP server access for your organization

**設定**: `chat.mcp.gallery.serviceUrl`、`chat.mcp.access`

GitHub Organization ポリシーを通じた MCP レジストリの構成がサポートされた。組織がカスタム MCP レジストリをセットアップし、インストール・起動できる MCP サーバーを制御できる。

- 構成されたレジストリからの MCP サーバーの閲覧・インストール
- アクセス制限有効時、レジストリ内のサーバーのみ起動可能
- 設定は Settings エディタで「(Managed by organization)」と表示

> 📷 [画像: 組織ポリシーで管理された MCP 設定](https://code.visualstudio.com/assets/updates/1_106/mcp-registry-policies.png)

**ステータス**: **[GA]**
**対象ユーザー**: 組織レベルで MCP サーバーを管理する管理者

#### Install MCP servers to workspace configuration

MCP サーバーのインストール時に、グローバルまたはワークスペース構成のいずれかを選択可能になった。

- 拡張機能ビューで MCP サーバーを右クリック → **Install (Workspace)** で `.vscode/mcp.json` に追加
- チームとの MCP サーバー共有が容易に

> 📷 [画像: MCP サーバーの Install (Workspace) オプション](https://code.visualstudio.com/assets/updates/1_106/mcp-install-workspace.png)

**ステータス**: **[GA]**
**対象ユーザー**: チームで MCP サーバーを共有するユーザー

#### Authentication: Client ID Metadata Document authentication flow

リモート MCP の認証で、[Client ID Metadata Document (CIMD)](https://www.ietf.org/archive/id/draft-parecki-oauth-client-id-metadata-document-00.html) 認証フローがサポートされた。MCP の将来の OAuth 標準であり、Dynamic Client Registration (DCR) よりもセキュアでスケーラブルなソリューション。

- CIMD をサポートする認可サーバーに接続する場合、VS Code が自動的に DCR の代わりに CIMD フローを使用

**ステータス**: **[GA]**
**対象ユーザー**: リモート MCP サーバーを利用するユーザー

#### Authentication: `WWW-Authenticate` scope step up

リモート MCP サーバーの `WWW-Authenticate` ヘッダーによる動的スコープエスカレーションがサポートされた。MCP サーバーが必要に応じて追加の権限を要求でき、すべてのスコープを事前に要求する必要がない。最小権限の原則に基づくセキュリティを提供。

**ステータス**: **[GA]**
**対象ユーザー**: リモート MCP サーバーを利用するユーザー

### Accessibility

#### 概要
音声タイムアウトのデフォルト無効化とチャット入力のアクセシビリティ改善が行われた。

#### 機能詳細

#### Speech timeout is disabled by default

**設定**: `accessibility.voice.speechTimeout`

`accessibility.voice.speechTimeout` のデフォルトが `0` に変更された。音声セッションが一定の遅延後に自動的に終了しなくなった（例: 一時停止してもチャットリクエストが自動送信されない）。以前のデフォルト（`2500`）に戻すことも可能。

**ステータス**: **[GA]**（動作変更）
**対象ユーザー**: 音声入力を利用するユーザー

#### Chat input improvements

チャット入力がアクティブなエージェントとモデルをより明確な順序でアナウンスするようになった。チャットアクセシビリティヘルプで **Delete** キーによるコンテキスト項目の削除についても案内が追加。

**ステータス**: **[GA]**
**対象ユーザー**: スクリーンリーダーを使用するユーザー

### Notebooks

#### 概要
ノートブック内でのセル横断検索がサポートされた。

#### 機能詳細

#### Notebook search

ノートブックでセル間の検索がサポートされた。キーバインド（`Ctrl+Shift+F3` / `Ctrl+Shift+F4`）で次・前のマッチにナビゲート可能。エディタでの検索と同様の操作。

> 🎬 [動画: ノートブックセルでの検索](https://code.visualstudio.com/assets/updates/1_106/notebookSearch.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: ノートブックを利用するユーザー

### Source Control

#### 概要
Git コミットメッセージのフォールディング、Source Control Graph ビューでの incoming/outgoing 変更表示、リファレンス比較、リポジトリ選択モード、リポジトリエクスプローラ（Experimental）が追加された。

#### 機能詳細

#### Folding support in git commit messages

**設定**: `git.verboseCommit`、`git.useEditorAsCommitInput`

エディタで Git コミットメッセージを作成する際にセクションのフォールディングが可能になった。`git.verboseCommit` と `git.useEditorAsCommitInput` を有効にして利用。

> 📷 [画像: コミットメッセージのフォールディング](https://code.visualstudio.com/assets/updates/1_106/commit-folding.png)

**ステータス**: **[GA]**
**対象ユーザー**: エディタでコミットメッセージを作成するユーザー

#### Graph incoming/outgoing changes

**設定**: `scm.graph.showIncomingChanges`、`scm.graph.showOutgoingChanges`

Source Control Graph ビューで incoming/outgoing 変更を簡単に確認できるようになった。アクティブブランチに incoming/outgoing 変更がある場合、グラフに「Incoming Changes」「Outgoing Changes」ノードが表示され、選択するとファイル一覧が表示される。

> 📷 [画像: Source Control Graph ビューの incoming/outgoing 変更ノード](https://code.visualstudio.com/assets/updates/1_106/graph-incoming-outgoing.png)

**ステータス**: **[GA]**
**対象ユーザー**: Source Control Graph を利用するユーザー

#### Graph compare references

Source Control Graph のコンテキストメニューに **Compare with...** コマンドが追加された。グラフの履歴アイテムを任意のブランチやタグと比較可能。

- **Compare with Remote** と **Compare with Merge Base** のショートカットコマンドも追加

**ステータス**: **[GA]**
**対象ユーザー**: Git ブランチ比較を行うユーザー

#### Repositories selection mode

**設定**: `scm.repositories.selectionMode`

Source Control Repositories ビューの選択モードを単一リポジトリまたは複数リポジトリに制御する設定。Graph ビューのタイトル内のリポジトリピッカーを廃止し、すべてのソースコントロールビューで共通のリポジトリピッカーを提供。

- 設定または Repositories ビューの「...」メニューから選択モードを切り替え可能

> 📷 [画像: シングル選択モードの Repositories ビュー](https://code.visualstudio.com/assets/updates/1_106/repositories-selection-mode.png)

**ステータス**: **[GA]**
**対象ユーザー**: 複数リポジトリを管理するユーザー

#### New Repository menu

拡張機能作者が `scm/repository` メニュー ID を使用して、Source Control Repositories ビューのインラインプロバイダ行にコマンドを追加可能。「inline」グループはインライン表示、その他は「...」メニューに表示。

**ステータス**: **[GA]**
**対象ユーザー**: SCM 拡張機能の開発者

#### Repositories explorer (experimental)

**設定**: `scm.repositories.explorer`（`true` で有効化）、`scm.repositories.selectionMode`（`single` に設定）

Repositories ビューの拡張で、各リポジトリの追加情報を表示する実験的機能。

- ブランチとタグの一覧表示
- 新規ブランチ・タグの作成
- 各ブランチ/タグへのアクション（チェックアウトなど）
- 今後のリリースでスタッシュ、リモートなどの情報を追加予定

> 📷 [画像: Repositories エクスプローラのブランチとタグ](https://code.visualstudio.com/assets/updates/1_106/repositories-explorer.png)

**ステータス**: **[Experimental]**
**対象ユーザー**: リポジトリ管理を強化したいユーザー

### Testing

#### 概要
テストカバレッジの未カバー行間のナビゲーションコマンドが追加された。

#### 機能詳細

#### Navigate uncovered lines in test coverage

テストカバレッジ確認時に、未カバー行間を簡単にナビゲートする2つの新コマンドが利用可能:

- **Go to Next Uncovered Line** — 次の未カバー行にジャンプ
- **Go to Previous Uncovered Line** — 前の未カバー行にジャンプ

**ステータス**: **[GA]**
**対象ユーザー**: テストカバレッジを重視する開発者

### Terminal

#### 概要
Terminal IntelliSense が約1.5年のプレビュー期間を経て GA 化。多数のカスタマイズオプションと新しいコマンド補完が追加された。シェル統合タイムアウト設定も統合。

#### 機能詳細

#### Terminal IntelliSense

Terminal IntelliSense がプレビュータグを外し、正式リリースとして Stable ユーザーに段階的にロールアウトされる。PowerShell、bash、zsh、fish でエディタ類似の IntelliSense を提供。

- パス補完はコアで処理
- `git` のような高度なコマンドではブランチ名の取得などをサポート
- `copilot` と `azd` CLI の補完が新たに追加
- Git コミット補完でコミットメッセージを表示
- 拡張 API がファイナライゼーション近く

**主なカスタマイズ設定:**
- `terminal.integrated.suggest.quickSuggestions` — コマンドライン内容に応じた自動表示
- `terminal.integrated.suggest.suggestOnTriggerCharacters` — `-` や `/` などのトリガー文字後に自動表示
- `terminal.integrated.suggest.runOnEnter` — Enter でコマンド実行（Tab ではなく）
- `terminal.integrated.suggest.selectionMode` — IntelliSense ポップアップのフォーカス動作
- `terminal.integrated.suggest.insertTrailingSpace` — 受け入れ後にスペースを挿入して再トリガー（新規）

その他多数の設定あり。利用にはシェル統合が有効であることが必要。明示的に有効化するには `terminal.integrated.suggest.enabled` を設定。

> 📷 [画像: ターミナルでの write コマンド補完](https://code.visualstudio.com/assets/updates/1_106/terminal-suggest-write.png)
> 🎬 [動画: cd コマンドのディレクトリ補完](https://code.visualstudio.com/assets/updates/1_106/terminal-suggest-cd.mp4)
> 🎬 [動画: git コマンドのブランチ名補完](https://code.visualstudio.com/assets/updates/1_106/terminal-suggest-git.mp4)
> 📷 [画像: Git コミット補完でのコミットメッセージ表示](https://code.visualstudio.com/assets/updates/1_106/terminal-suggest-commit.png)

**ステータス**: **[GA]**
**対象ユーザー**: ターミナルを日常的に利用するすべてのユーザー

#### Consolidated shell integration timeout setting

**設定**: `terminal.integrated.shellIntegration.timeout`

シェル統合の準備完了を待機するタイムアウト設定が `terminal.integrated.shellIntegration.timeout` に一本化された。executeCommand API や Copilot ターミナルツールによるコマンド実行にも適用。

- `chat.tools.terminal.shellIntegrationTimeout` は廃止（この設定に統合）

**ステータス**: **[GA]**
**対象ユーザー**: ターミナルユーザー

### Authentication

#### 概要
拡張機能アカウント管理の発見性向上、classic Microsoft 認証の最終バージョン、デバイスコードフロー、アカウント管理コマンドなど、認証関連の改善が行われた。

#### 機能詳細

#### Manage extension account preferences discoverability

**Manage Extension Account Preferences** コマンドの発見性が向上。コマンドパレットと拡張機能コンテキストメニューに加え、Account Menu にも **Manage Language Model Access** の横に表示されるようになった。

> 📷 [画像: Account メニューでの Manage Extension Account Preferences](https://code.visualstudio.com/assets/updates/1_106/manageExtensionAccountPref.png)

**ステータス**: **[GA]**
**対象ユーザー**: 拡張機能のアカウントアクセスを管理するユーザー

#### Last version of `classic` Microsoft authentication - use `msal-no-broker` if you experience issues

`microsoft-authentication.implementation` の `classic` オプションが本リリースを最後に削除される。

- `msal` — ブローカー認証付き MSAL を使用（デフォルト）
- `msal-no-broker` — ブローカーなし MSAL を使用
- `classic` — MSAL なしのクラシックフロー（今回が最終）
- 問題がある場合は `msal-no-broker` を使用

**ステータス**: **[GA]**（Breaking Change — `classic` の廃止）
**対象ユーザー**: Microsoft 認証の `classic` オプションを使用していたユーザー

#### Device code flow for Microsoft authentication

Microsoft 認証で非ブローカーシナリオ向けのデバイスコードフローがサポートされた。他の認証方法が失敗した場合に自動的にフォールバックし、別のデバイスでコードを入力して認証を完了できる。リモート開発環境で特に有用。

**ステータス**: **[GA]**
**対象ユーザー**: リモート開発環境のユーザー

#### Manage accounts command

**Accounts: Manage Accounts** コマンドがコマンドパレットから利用可能になった。Account メニューが非表示またはアクセスしにくい場合に直接アカウント管理を行える。

- すべてのアクティブアカウントを一覧表示
- **Manage Trusted Extensions** — 選択したアカウントにアクセスできる拡張機能を制御
- **Manage Trusted MCP Servers** — MCP サーバーのアクセス権限を管理
- **Sign Out** — アカウントからサインアウト

> 📷 [画像: Manage Accounts メニュー](https://code.visualstudio.com/assets/updates/1_106/manage-accounts-menu.png)

**ステータス**: **[GA]**
**対象ユーザー**: すべてのユーザー

### Languages

#### 概要
Python 関連の複数の改善（Poetry パス設定、venv 作成改善、Copilot ホバーサマリーの docstring 化、ローカライズ、ワイルドカードインポート変換）と dotenv ファイルの基本サポートが追加された。

#### 機能詳細

#### Python Environments Extension: Support for python.poetryPath setting

Python Environments 拡張が既存の `python.poetryPath` ユーザー設定を参照するようになった。使用する Poetry 実行ファイルを指定でき、Poetry 環境の管理時に検索・選択される。

**ステータス**: **[GA]**
**対象ユーザー**: Poetry を使用する Python 開発者

#### Python Environments Extension: Improved venv creation: dev-requirements.txt detection

新しい仮想環境の作成時に、`requirements.txt` と `dev-requirements.txt` の両方が検出され、依存関係が自動的にインストールされるようになった。

**ステータス**: **[GA]**
**対象ユーザー**: Python 開発者

#### Add Copilot Hover Summaries as docstring

AI 生成のドキュメントを **Add as docstring** コマンドでコードに直接 docstring として追加可能に。関数やクラスのサマリー生成後、シンボル定義にホバーしてコマンドにアクセスする。

> 🎬 [動画: Copilot ホバーサマリーの docstring 追加](https://code.visualstudio.com/assets/updates/1_106/pylance-add-hover-summaries-as-docstring.mp4)

**ステータス**: **[GA]**
**対象ユーザー**: Python 開発者

#### Localized Copilot Hover Summaries

Pylance の Copilot ホバーサマリーが VS Code の表示言語設定に従うようになった。エディタに設定した言語で AI 生成サマリーが表示される。

> 📷 [画像: ポルトガル語で生成されたホバーサマリー](https://code.visualstudio.com/assets/updates/1_106/hover-summaries-in-portuguese.png)

**ステータス**: **[GA]**
**対象ユーザー**: 非英語環境の Python 開発者

#### Convert wildcard imports Code Action

Pylance にワイルドカードインポート（`from module import *`）を明示的なインポートに変換する Code Action が追加された。`Ctrl+.`（macOS: `Cmd+.`）でワイルドカードインポート行にカーソルを置いて **Convert to explicit imports** を選択。

> 📷 [画像: ワイルドカードインポート変換の Code Action](https://code.visualstudio.com/assets/updates/1_106/wildcard-import.png)

**ステータス**: **[GA]**
**対象ユーザー**: Python 開発者

#### dotenv

dotenv ファイル（`.env`）の基本的なビルトインサポートが追加された。アプリケーションの環境変数定義に広く使用されるファイル形式。

**ステータス**: **[GA]**
**対象ユーザー**: `.env` ファイルを使用する開発者

### Contributions to extensions

#### GitHub Pull Requests

[GitHub Pull Requests](https://marketplace.visualstudio.com/items?itemName=GitHub.vscode-pull-request-github) 拡張に以下の新機能が追加された。

- AI 生成の PR 説明文（`githubPullRequests.pullRequestDescription` を `copilot` に設定）がリポジトリの PR テンプレートを参照するようになった
- Pull Requests ビューでドラフトがイタリック表示に（以前の `[DRAFT]` プレフィックスから変更）
- URL から PR を開くことが可能（例: `vscode-insiders://github.vscode-pull-request-github/checkout-pull-request?uri=...`）

詳細は [changelog for the 0.122.0](https://github.com/microsoft/vscode-pull-request-github/blob/main/CHANGELOG.md#01220) を参照。

### Preview Features

#### 概要
Language Models エディタのプレビューが導入された。

#### 機能詳細

#### Language Models editor

GitHub Copilot Chat の利用可能な言語モデルを一元管理する新しい **Language Models** エディタ。チャットモデルピッカーまたは **Chat: Manage Language Models** コマンドから開く。

> **注意:** この機能は [VS Code Insiders](https://code.visualstudio.com/insiders/) でのみ利用可能。

- プロバイダ別にモデルを整理して表示
- モデルの機能（tools、vision、agent）、コンテキストサイズ、マルチプライヤー情報を表示
- テキスト検索、プロバイダフィルタ（`@provider:"OpenAI"`）、機能フィルタ（`@capability:tools`）、表示フィルタ（`@visible:true/false`）でフィルタリング

> 📷 [画像: Language Models エディタ](https://code.visualstudio.com/assets/updates/1_106/language-models-editor.png)

##### Manage model visibility
チャットモデルピッカーに表示するモデルを目のアイコンで切り替え。使用頻度の低いモデルを非表示にしてピッカーを整理可能。

> 📷 [画像: モデル表示切り替えアイコン](https://code.visualstudio.com/assets/updates/1_106/toggle-language-model-visibility.png)

##### Add models from installed providers
**Add Models...** ボタンでインストール済みのモデルプロバイダ（Copilot、Anthropic、Azure、Google、Groq、Ollama、OpenAI 等）からモデルを追加・設定。

> 📷 [画像: Add Models ドロップダウン](https://code.visualstudio.com/assets/updates/1_106/add-language-models-dropdown.png)

**ステータス**: **[Preview]**
**対象ユーザー**: 複数の言語モデルを管理するユーザー

## 設定項目まとめ

### Agents

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `chat.agentSessionsViewLocation` | Agent Sessions ビューの配置（デフォルト / `single-view`） | — | GA |

### Chat

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `chat.tools.terminal.blockDetectedFileWrites` | ファイル書き込みリダイレクション検出時に自動承認をブロック | — | Experimental |
| `chat.tools.terminal.ignoreDefaultAutoApproveRules` | デフォルト自動承認ルールを無効化 | — | Experimental |
| `chat.tools.terminal.outputLocation` | ターミナル出力の表示場所 | `none` | Experimental |
| `chat.openEditedFilesAutomatically` | 編集ファイルの自動オープン | `false`（変更） | GA |
| `accessibility.openChatEditedFiles` | チャット編集ファイルの自動オープン（旧動作の復元用） | — | GA |
| `chat.agent.thinkingStyle` | 推論トークンの表示スタイル | `fixedScrolling` | Experimental |
| `chat.agent.thinking.collapsedTools` | ツール呼び出しを思考 UI に統合 | — | Experimental |
| `inlineChat.enableV2` | Inline chat v2 の有効化 | `false` | Preview |
| `chat.extensionUnification.enabled` | 拡張機能統合の有効化 | — | GA |
| `chat.promptFilesRecommendations` | ウェルカムビューのプロンプト推奨設定 | — | GA |

### MCP

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `chat.mcp.gallery.serviceUrl` | MCP レジストリエンドポイント URL | — | GA |
| `chat.mcp.access` | MCP サーバーアクセス制限 | — | GA |

### Accessibility

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `accessibility.voice.speechTimeout` | 音声タイムアウト | `0`（変更） | GA |

### Source Control

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `git.verboseCommit` | 詳細コミット情報の表示 | — | GA |
| `git.useEditorAsCommitInput` | エディタをコミット入力に使用 | — | GA |
| `scm.graph.showIncomingChanges` | Graph ビューでの incoming 変更表示 | `true` | GA |
| `scm.graph.showOutgoingChanges` | Graph ビューでの outgoing 変更表示 | `true` | GA |
| `scm.repositories.selectionMode` | リポジトリ選択モード（single / multiple） | — | GA |
| `scm.repositories.explorer` | リポジトリエクスプローラの有効化 | `false` | Experimental |

### Terminal

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `terminal.integrated.suggest.enabled` | Terminal IntelliSense の有効化 | — | GA |
| `terminal.integrated.suggest.quickSuggestions` | 自動サジェスト | — | GA |
| `terminal.integrated.suggest.suggestOnTriggerCharacters` | トリガー文字後のサジェスト | — | GA |
| `terminal.integrated.suggest.runOnEnter` | Enter でコマンド実行 | — | GA |
| `terminal.integrated.suggest.selectionMode` | IntelliSense フォーカス動作 | — | GA |
| `terminal.integrated.suggest.insertTrailingSpace` | 受け入れ後にスペース挿入（新規） | — | GA |
| `terminal.integrated.shellIntegration.timeout` | シェル統合タイムアウト（統合） | — | GA |

### Authentication

| 設定キー | 説明 | デフォルト | ステータス |
|---|---|---|---|
| `microsoft-authentication.implementation` | Microsoft 認証実装方式（`classic` は廃止） | `msal` | GA |

## Breaking Changes / 非推奨化

| 項目 | 内容 |
|---|---|
| `classic` Microsoft 認証の廃止 | `microsoft-authentication.implementation` の `classic` オプションが本リリースを最後に削除。問題がある場合は `msal-no-broker` を使用 |
| macOS 11.0 サポートの終了 | VS Code 1.106 が macOS 11.0 (Big Sur) をサポートする最後のリリース |
| `chat.tools.terminal.shellIntegrationTimeout` の廃止 | `terminal.integrated.shellIntegration.timeout` に統合 |
| 編集ファイルの自動オープン無効化 | エージェント編集ファイルの自動オープンがデフォルトで無効に変更。`accessibility.openChatEditedFiles` で復帰可能 |
| `accessibility.voice.speechTimeout` のデフォルト変更 | デフォルトが `2500` から `0` に変更（音声セッションが自動終了しなくなった） |

## 開発者・拡張機能作成者向け

### Finalized APIs

#### ID token in authentication sessions

`AuthenticationSession` インターフェースにオプショナルな `idToken` プロパティが追加された。認証プロバイダがアクセストークンに加えて ID トークンを返すことが可能に。Microsoft 認証プロバイダがこのフィールドを返す。

```typescript
export interface AuthenticationSession {
  readonly idToken?: string;
}
```

#### Git extension `getRepositoryWorkspace` API

Git 拡張に、git リポジトリリモートに関連付けられたフォルダを取得する新しい API が追加された。ユーザーが git リモート付きのフォルダを開く際にリポジトリリモートとフォルダのマッピングをキャッシュして動作。

#### View containers in Secondary Side Bar

拡張機能作者が `secondarySidebar` contribution point を使用して Secondary Side Bar にビューコンテナを登録可能に。Chat などの組み込みビューと並べてカスタムビューを配置できる。

```json
{
  "contributes": {
    "viewsContainers": {
      "secondarySidebar": [
        {
          "id": "myExtensionViews",
          "title": "My Extension",
          "icon": "$(extensions)"
        }
      ]
    }
  }
}
```

### Proposed APIs

#### Quick Pick and Quick Input improvements

Quick Pick / Quick Input API に複数の新機能が追加（すべてプロポーザル段階）。

> 🎬 [動画: Quick Pick API の新機能](https://code.visualstudio.com/assets/updates/1_106/quick-pick-api.mp4)

> ⚠️ これらの API はプロポーザル段階であり、今後変更または削除される可能性があります。

##### Toggle button support

> プロポーザルファイル: Quick Input の `toggles` プロパティ

Quick Pick / Quick Input にトグルボタンを追加可能。パスワード表示切り替えなどのシナリオに対応。`QuickInputButtonLocation.Input` で入力ボックス内にボタンを描画。

##### Prompt support for Quick Pick

> プロポーザルファイル: Quick Pick の `prompt` プロパティ

Quick Pick に `prompt` プロパティを追加。入力ボックスの下に永続的なガイダンステキストを表示。ユーザーの入力中も表示され続ける。

##### File icons for Quick Pick items

> プロポーザルファイル: `QuickPickItem` の `resourceUri` プロパティ

Quick Pick アイテムにリソース URI を設定すると、VS Code が自動的にラベル、説明、アイコンを導出。ファイルアイコンテーマに基づくアイコン表示が可能。

#### GitHub-style alerts in MarkdownStrings

> ⚠️ この API はプロポーザル段階であり、今後変更または削除される可能性があります。
> プロポーザルファイル: `MarkdownString` の `supportAlertSyntax` プロパティ

`MarkdownString` で GitHub スタイルのアラート構文（`> [!NOTE]`、`> [!TIP]`、`> [!IMPORTANT]`、`> [!WARNING]`、`> [!CAUTION]`）のレンダリングをサポート。コメントなどの UI 要素内で使用可能。

> 📷 [画像: コメントスレッド内の GitHub スタイルアラート](https://code.visualstudio.com/assets/updates/1_106/markdown-alerts.png)

#### MarkdownString support in TreeItem labels

> ⚠️ この API はプロポーザル段階であり、今後変更または削除される可能性があります。
> プロポーザルファイル: `TreeItem` ラベルでの `MarkdownString` サポート

ツリービューアイテムのラベルに `MarkdownString` を使用可能。codicon やテキストフォーマット（イタリック、ボールド、取り消し線）の限定的なサブセットをサポート。

```typescript
const itemWithIcon = new vscode.TreeItem({ label: new vscode.MarkdownString('$(star) Starred item', true) });
const italicItem = new vscode.TreeItem({ label: new vscode.MarkdownString('_Italic item_') });
```

> 📷 [画像: Markdown ラベルのツリービュー](https://code.visualstudio.com/assets/updates/1_106/markdown-labels.png)

## エンジニアリング・基盤改善

### Exploration of automated UX PR testing

UI 変更を含む PR のレビューを自動化するワークフローが導入された。`~copilot-video-please` ラベルを PR に追加すると:

- PR ブランチから VS Code をビルド
- GitHub Copilot CLI が [playwright-mcp](https://github.com/microsoft/playwright-mcp) を活用して変更を操作し動画を記録
- Playwright トレースを生成
- PR コメントとして結果を投稿

現時点では動画はチームメンバーのみアクセス可能。

### macOS 11.0 support has ended

VS Code 1.106 が macOS 11.0 (macOS Big Sur) をサポートする最後のリリースとなった。

## Notable fixes

- [vscode#258236](https://github.com/microsoft/vscode/issues/258236) — 拡張機能インストール時のリクエストタイムアウト設定を追加
- [vscode#272945](https://github.com/microsoft/vscode/issues/272945) — タスクが `onDidStartTerminalShellExecution` をトリガーしない問題を修正
- [vscode#273372](https://github.com/microsoft/vscode/issues/273372) — `.gitignore` ファイルで `/**` が `*/` で自動クローズされる問題を修正
- [vscode#243584](https://github.com/microsoft/vscode/issues/243584) — pwsh/conpty で最初の入力が無視される問題を修正
- [vscode#271952](https://github.com/microsoft/vscode/pull/271952) — Copilot の configure instructions クイックピックがワークスペースレベルのエージェント指示ファイルを表示しない問題を修正
- [vscode#274631](https://github.com/microsoft/vscode/pull/274631) — Windows で中間証明機関のロードを改善

## コミュニティ貢献

### 主な貢献者と貢献内容

| 貢献者 | 主な貢献 |
|---|---|
| **Simon Siefke** | メモリリーク修正を8件実施（gettingStarted、output view、notebook text model、InlayHints、chat session tracker、terminal、sticky scroll、chat welcome） |
| **Mingpan** | インライン diff での削除チャンクのコピー選択機能 ([PR #267991](https://github.com/microsoft/vscode/pull/267991)) |
| **obrobrio2000** | テストカバレッジの未カバー行ナビゲーションコマンド ([PR #269505](https://github.com/microsoft/vscode/pull/269505))、HTML エンドタグ提案の無効化設定 |
| **remcohaszing** | dotenv サポートの追加 ([PR #273074](https://github.com/microsoft/vscode/pull/273074)) |
| **Daniel Bayley** | `TM_DIRECTORY_BASE` スニペット変数の追加 ([PR #270262](https://github.com/microsoft/vscode/pull/270262)) |
| **tamuratak** | プロンプトファイルと auto-attach instructions の適用修正、customModes.custom の undefined ガード |
| **JeffreyCA** | Azure Developer CLI (azd) の Fig spec 追加 |
| **David Barbet** | セマンティックトークン範囲リフレッシュ通知のビューポート貢献への接続 |

### Issue tracking 貢献者

- RedCMD、John Murray、Andrii Dieiev、Alberto Santini、Accurio

### その他のリポジトリへの貢献

- **vscode-copilot-chat**: ビルトインツールセット、カスタム VSC モデルプロンプト、404 エラーハンドリング改善、NuGet MCP インストールフロー
- **vscode-pull-request-github**: PR テンプレートの AI 活用、アバター表示修正、ブランチリストタイムアウト設定
- **node-pty**: ネイティブアドオンのプリビルドディレクトリからの直接ロード

## まとめと所感

VS Code 1.106 はエージェント体験の基盤を大幅に整備したリリースである。Agent Sessions ビューのデフォルト有効化と Plan agent の導入により、AI との協業ワークフローがより構造化された。Terminal IntelliSense の GA 化は長期にわたるプレビュー期間の集大成であり、ターミナル作業の生産性を大きく向上させる。セキュリティ面では tree-sitter パーサーの統合やツール承認の改善により、エージェントへのタスク委譲をより安心して行えるようになった。macOS 11.0 サポートの終了と `classic` Microsoft 認証の廃止は、環境を最新に保つことの重要性を改めて示している。
